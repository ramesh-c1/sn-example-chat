#!/usr/bin/env node
require('strong-agent').profile(
  process.env.NODEFLY_KEY,
  ['com.strongloop.example.chat']
);

// Default clustering to on, it can still be over-ridden by config, env, or
// command line
var options = {
  size: 'default'
};

require('../index')(options);
